[project]
name = "baby-backend"
version = "0.1.0"
description = "A minimal FastAPI backend service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.137.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.18",
]
